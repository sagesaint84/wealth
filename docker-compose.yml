services:
  dashboard:
    build: .
    container_name: wealth
    env_file:
      - ./.env
    ports:
      - "4829:4829"
    volumes:
      # 실제 보유종목 데이터는 컨테이너 밖에 영구 보관
      - ./data:/app/data
    restart: unless-stopped
